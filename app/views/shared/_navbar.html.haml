#navbar
  .col-xs-12
    %h1= @application.name
    .links
      = link_to application_path(@application.id) do
        Installation instructions
      -#= link_to new_application_idea_path(@application.id), class: 'list-group-item' do
        %i.fa.fa-plus
        New idea
      = link_to application_ideas_path(@application.id) do
        Show ideas
      = link_to application_path(@application.id, anchor: 'customize') do
        Customization
      = link_to preview_application_path(@application.id) do
        My widget
      - if @application.faqs_enabled
        = link_to application_faqs_path(@application.id) do
          FAQ
