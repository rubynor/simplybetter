.col-md-3
  .list-group
    = link_to application_path(@application.id), class: 'list-group-item' do
      %i.icon-smile
      Installation instructions
    = link_to new_application_feature_path(@application.id), class: 'list-group-item' do
      %i.icon-plus-sign
      Create a new feature request
    = link_to administrate_group_application_path(@application.id), class: 'list-group-item' do
      %i.icon-list-alt
      Select features for voting
    = link_to applications_path, class: 'list-group-item' do
      %i.icon-circle-arrow-left
      Go back to applications overview