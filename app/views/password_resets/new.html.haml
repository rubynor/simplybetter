%h1 Send reset instructions
%p Enter your email below
.form
  -if flash.alert
    .notice
      = flash.alert
  = form_tag password_resets_path, method: :post do
    .field
      %label
        E-mail
      = email_field_tag :email, params[:email], placeholder: 'your email'
    .field
      .pull-right
        .actions= submit_tag 'Reset password'
