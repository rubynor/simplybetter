%h2 New application
= form_for Application.new, url: create_application_customer_path(current_customer) do |f|
  = f.label :name
  = f.text_field :name
  = f.submit

%h2 Existing applications

- @applications.each do |application|
  %b= application.name
  Token:#{application.token}
  %br/
  - if application.ideas.count > 0
    %ul
      - application.ideas.each do |idea|
        %li
          = idea.votes_count
          = idea.title
%br/
