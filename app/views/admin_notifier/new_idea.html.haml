= render '/shared/email_header'

.idea-layout
  .idea-item
    %p
      = "'#{@creator.name}'"
      just submittet an idea to your SimplyBetter project
      %strong= "'#{@idea.application.name}'"
    %hr
    %a(href="#{WidgetPath.path}/widget?appkey=#{@idea.application.token}&email=#{@receiver.email}&name='#{@receiver.name}'#/widget/ideas/#{@idea.id}")
      %h1
        = @idea.title
    %p.desc.ideaView
      = @idea.description
    %hr

= render '/shared/email_footer'
